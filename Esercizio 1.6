/*implementare la funzione swap(int *a, int *b)che scambi il contenuto delle due variabili.
Scrivere poi un programma che legga da input il valore di due variabili intere a e b e utilizzi 
la funzione swapper scambiare i loro valori. Il programma deve quindi stampare il valore di a e b
dopo questa operazione.
Le due righe dell'input contengono il valore di a e b.
Le due righe dell'output contengono il valore di a e b dopo lo scambio.*/


#include <iostream>

using namespace std;

void swap(int*a, int *b){
	int *c= b;
	b = a;
	a = b;
}

int main(){
	int A =1;
	int B = 2;
	swap(A, B);
	cout << A << ' ' << B;
	return 0;
}
