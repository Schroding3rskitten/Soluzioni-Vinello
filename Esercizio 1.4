/*Scrivere un programma che legga da input gliNinteri di un array A. 
Il programma deve invertireA in loco (cioè senza utilizzare un array di appoggio),
ossia scambiare il contenuto della prima e dell'ultima cella, 
della seconda edella penultima, ecc. Si assuma che N<=10000.La prima riga dell'input è il valore N.  
SeguonoNinteri, uno per riga.Il  programma  stampa  in  output  gli  elementi  dell'array  invertito, 
uno  per riga.*/


#include <iostream> 

using namespace std;


int main(){
	int size, m;
	cin >> size;
	int A[size];
	for(int j=0; j<size; ++j){
		cin >> m;
		A[j] = m;
	}
	for (int i=size-1; i>=0; i--){
		cout << A[i] << " ";
	}
	return 0;
}
